from math import *


def multiplier(width, range):
    if(isinf(value)):
        return 0    
    return (2**width) / range


def digitize(value, multi):
    if(isinf(value)):
        return 0
    return int(floor(value * multi))


def digitizeR(value, multi):
    if(isinf(value)):
        return 0
    return int(round(value * multi))