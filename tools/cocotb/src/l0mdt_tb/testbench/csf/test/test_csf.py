
##################################################
# autogenerated file
# created by tb create on: 12-Oct-2021 (14:14:59)
# created by tb create for test: csf
# Author:Priya Sundararajan
# Email:priya.sundararajan@cern.ch
##################################################


#!/usr/bin/env python3

import sys
import os
import json

import cocotb
from cocotb.clock import Clock
from cocotb.triggers import ClockCycles, RisingEdge, Combine, Timer, with_timeout
from cocotb.result import TestFailure, TestSuccess

import l0mdt_tb.testbench.csf.csf_wrapper as wrapper
from l0mdt_tb.testbench.csf import csf_ports 
CsfPorts=csf_ports.CsfPorts()

# CREATORSOFTWAREBLOCKimport l0mdt_tb.testbench.csf.csf_block as csf_block

from l0mdt_tb.utils import test_config
from l0mdt_tb.utils import events
from l0mdt_tb.utils.fifo_wrapper import FifoDriver, FifoMonitor


def initialize_spybuffers(fifos=[]):

    for ififo, fifo in enumerate(fifos):
        fifo.read_enable <= 0
        fifo.write_enable <= 0
        fifo.empty <= 1
        fifo.almost_full <= 0
        fifo.read_data <= 0
        fifo.write_data <= 0


def initialize_dut(dut,config):

    ##
    ## initialize the FIFOs
    ##
    cocotb_inputs  = 0
    cocotb_outputs = 0
    for i in range(CsfPorts.n_input_interfaces):
        cocotb_inputs = CsfPorts.get_input_interface_ports(i) + cocotb_inputs

    for i in range(CsfPorts.n_output_interfaces):
        cocotb_outputs = CsfPorts.get_output_interface_ports(i) + cocotb_outputs


    input_fifos = [x.spybuffer for x in dut.input_spybuffers]

    cocotb_input_interfaces = CsfPorts.n_input_interfaces;

    n_inputs_ok             = len(input_fifos) == cocotb_inputs

    output_fifos = [x.spybuffer for x in dut.output_spybuffers]
    n_outputs_ok = len(output_fifos) == cocotb_outputs
    n_io_ports_ok = n_inputs_ok and n_outputs_ok
    if not n_io_ports_ok:
        raise Exception(
            f"ERROR # of Csf io ports differ between CocoTB and RTL simulation:\n -> CocoTB expects (IN,OUT)=({cocotb_inputs},{cocotb_outputs})\n -> RTL expects (IN,OUT)=({len(input_fifos)},{len(output_fifos)})"
        )

    initialize_spybuffers(fifos=input_fifos)
    initialize_spybuffers(fifos=output_fifos)


@cocotb.coroutine
def reset(dut):

    """
    Resets the testbench, having reset active LOW.
    """

    dut.reset_n <= 0
    yield ClockCycles(dut.clock, 10)
    dut.reset_n <= 0
    yield ClockCycles(dut.clock, 10)
    dut.reset_n <= 1
    yield ClockCycles(dut.clock, 20)

##
## TEST
##
@cocotb.test()
def csf_test(dut):

    ##
    ## first grab the testbench configuration
    ##
    config = test_config.get_config()

    ##
    ## process input arguments for this test
    ##
    input_args                       = config["input_args"]
    test_vectors                     = config["testvectors"]
    num_events_to_process            = int(input_args["n_events"])
    event_level_detail_in_sumary     = bool(input_args["event_detail"])
    run_config                       = config["run_config"]
    output_dir_name                  = run_config["output_directory_name"]
    output_dir                       = f"{os.getcwd()}/../../../../../test_output/{output_dir_name}"
    master_tv_file                   = test_config.get_testvector_file_from_config(config)

    test_config.read_io_config(config['testvectors'],CsfPorts)

    #Test Specific JSON Parameters
    heg2sfslc_ii      = config['testvectors']['inputs'][0]["heg2sfslc_ii"]
    csf_init_cycles   = int(input_args["csf_init_cycles"])
    max_hits_in_evt   = int(input_args["max_hits_in_evt"])


    # CREATORSOFTWAREBLOCK##
    # CREATORSOFTWAREBLOCK## start the software block instance
    # CREATORSOFTWAREBLOCK##
    # CREATORSOFTWAREBLOCKcsf_block_instance = csf_block.csfBlock(dut.clock, "csfBlock")
    # CREATORSOFTWAREBLOCKfor i, io in enumerate(CsfPorts.Inputs):
    # CREATORSOFTWAREBLOCK    csf_block_instance.add_fifo(
    # CREATORSOFTWAREBLOCK        dut.input_spybuffers[i].spybuffer,
    # CREATORSOFTWAREBLOCK        dut.clock,
    # CREATORSOFTWAREBLOCK        f"{csf_block_instance.name}_Input_{i}",
    # CREATORSOFTWAREBLOCK        io,
    # CREATORSOFTWAREBLOCK        direction="in",
    # CREATORSOFTWAREBLOCK    )
    # CREATORSOFTWAREBLOCKfor i, io in enumerate(CsfPorts.Outputs):
    # CREATORSOFTWAREBLOCK    csf_block_instance.add_fifo(
    # CREATORSOFTWAREBLOCK        dut.output_spybuffers[i].spybuffer,
    # CREATORSOFTWAREBLOCK        dut.clock,
    # CREATORSOFTWAREBLOCK        f"{csf_block_instance.name}_Output_{i}",
    # CREATORSOFTWAREBLOCK        io,
    # CREATORSOFTWAREBLOCK        direction="out",
    # CREATORSOFTWAREBLOCK    )
    # CREATORSOFTWAREBLOCKcsf_block_instance.start()



    ##
    ## setup the clock and start it
    ##
    sim_clock = Clock(
        dut.clock, int(input_args["clock_period"]), input_args["clock_time_unit"]
    )
    cocotb.fork(sim_clock.start())

    ##
    ## initialize the DUT to known state
    ##
    initialize_dut(dut,config)

    ##
    ## reset
    ##
    dut._log.info("Resetting DUT")
    yield reset(dut)




    ##
    ## initialize the Csf block wrapper
    ##
    csf_wrapper = wrapper.CsfWrapper(
        clock=dut.clock,
        name=f"CsfWrapper",
    )

    ##
    ## get testvector format
    ##
    (
        input_tvformats,
        output_tvformats,
    ) = test_config.get_tvformats_from_config(config)


    (
        input_tvtype
    ) = test_config.get_tvtype_from_config(config)

    sb_iport_index = 0
    for n_ip_intf in range(CsfPorts.n_input_interfaces): # Add concept of interface
        for io in range(CsfPorts.get_input_interface_ports(n_ip_intf)):
            driver = FifoDriver(
                dut.input_spybuffers[sb_iport_index].spybuffer,
                dut.clock,
                "Csf",
                input_tvformats[n_ip_intf],
                str(io),
                write_out=True,
                out_dir=output_dir
            )
            sb_iport_index = sb_iport_index + 1
            csf_wrapper.add_input_driver(driver, n_ip_intf, io) #Add interface

    sb_oport_index = 0
    for n_op_intf in range(CsfPorts.n_output_interfaces):
        for io in range(CsfPorts.get_output_interface_ports(n_op_intf)): #Outputs):
            active = True
            monitor = FifoMonitor(
                dut.output_spybuffers[sb_oport_index].spybuffer,
                dut.clock,
                "Csf",
                output_tvformats[n_op_intf],
                str(io),
                callbacks=[],
                write_out=True,
                out_dir=output_dir
            )
            sb_oport_index = sb_oport_index + 1
            csf_wrapper.add_output_monitor(monitor, n_op_intf, io, active=active)
    csf_wrapper.sort_ports()

    #Read TV file
    tv_bcid_list = events.read_tv(
        filename=master_tv_file,
        n_to_load=num_events_to_process
        )

    ###Get Input Test Vector List for Ports across all input interfaces##
    input_tv_list         = []
    single_interface_list = []
    i_eof                 = []
    for n_ip_intf in range(CsfPorts.n_input_interfaces): # Add concept of interface
        if input_tvtype[n_ip_intf] != "control" :
            single_interface_list = (events.parse_tvlist(
                tv_bcid_list,
                tvformat=input_tvformats[n_ip_intf],
                n_ports = CsfPorts.get_input_interface_ports(n_ip_intf),
                n_to_load=num_events_to_process,
                station_ID=CsfPorts.config_inputs['station_id'][n_ip_intf],
                tv_type=input_tvtype[n_ip_intf]
            ))
        else:
            single_interface_list.clear()
            for i in range(num_events_to_process):
                i_eof.append(1)
            single_interface_list.append(i_eof)

        if(n_ip_intf == 0): #HEG2SFSLC
            single_interface_list_delay = events.modify_tv(single_interface_list, heg2sfslc_ii)
            for io in range(CsfPorts.get_input_interface_ports(n_ip_intf)):
                input_tv_list.append(single_interface_list_delay[io])
        elif(n_ip_intf == 1): #HEG2SFHIT
            hits_in_event = []  
            hits_zero_padding = []
            for io in range(len(single_interface_list)): #(CsfPorts.get_input_interface_ports(n_ip_intf)):
                for e_i in range(len(single_interface_list[io])):
                    hits_in_event.append(len(single_interface_list[io]))
                    hits_zero_padding.append(heg2sfslc_ii - hits_in_event[e_i] - csf_init_cycles)
                single_interface_list_dly_0 = events.modify_tv_padzeroes(single_interface_list[io],'begin',csf_init_cycles)
                single_interface_list_dly_1 = events.modify_tv_padzeroes(single_interface_list_dly_0, 'end', hits_zero_padding)
                single_interface_list_dly_2 = events.flatten_list(single_interface_list_dly_1)
            for io in range(CsfPorts.get_input_interface_ports(n_ip_intf)):
                input_tv_list.append(single_interface_list_dly_2[io])
        elif (n_ip_intf == 2): #CSF_CTRL
            i_eof_zero_padding = csf_init_cycles + max_hits_in_evt
            i_eof_ii           = heg2sfslc_ii
            single_interface_list_dly_0 = events.modify_tv(single_interface_list, i_eof_ii)
            single_interface_list_dly_1 = events.modify_tv_padzeroes(single_interface_list_dly_0, 'begin', i_eof_zero_padding)
            input_tv_list.append(single_interface_list_dly_1[0])
            
            

   ###Get Output Test Vector List for Ports across all output interfaces##
    output_tv_list        =  []
    single_interface_list = []
    for n_op_intf in range(CsfPorts.n_output_interfaces): # Add concept of interface
        single_interface_list = (events.parse_tvlist(
            tv_bcid_list,
            tvformat=output_tvformats[n_op_intf],
            n_ports = CsfPorts.get_output_interface_ports(n_op_intf),
            n_to_load=num_events_to_process,
            station_ID=CsfPorts.config_outputs['station_id'][n_op_intf],
            tv_type="value"
            ))
        output_tv_list.append(single_interface_list)



    ##
    ## send input events
    ##
    dut._log.info("Sending input events")
    send_finished_signal = csf_wrapper.send_input_events(
        input_tv_list,
        n_to_send=num_events_to_process
    )

    if not send_finished_signal:
        raise cocotb.result.TestFailure(
            f"ERROR Event sending timed out! Number of expected inputs with events = {len(send_finished_signal)}"
        )
    try:
        yield with_timeout(Combine(*send_finished_signal),  num_events_to_process*20, "us")
    except Exception as ex:
        raise cocotb.result.TestFailure(
            f"ERROR Timed out waiting for events to send: {ex}"
        )
    dut._log.info("Sending finished!")


    #Block Latency
    yield ClockCycles(dut.clock, 100*num_events_to_process)
    ##

    ##
    ## perform testvector comparison test
    ##
    all_tests_passed = True
    all_test_results = []
    recvd_events_intf = []
    for n_op_intf in range(CsfPorts.n_output_interfaces):
        recvd_events     = [["" for x in range(num_events_to_process)]for y in range(CsfPorts.get_output_interface_ports(n_op_intf))]
        for n_oport, oport in enumerate(csf_wrapper.output_ports(n_op_intf)):

            ##
            ## extract the observed data for this output
            ##
            monitor, io, is_active = oport
            words = monitor.observed_words

            recvd_events[n_oport] = words
            cocotb.log.info(
                f"Output for interface {n_op_intf} : port num {n_oport} received {len(recvd_events[n_oport])} events"
            )
        recvd_events_intf.append(recvd_events)

    ##
    ## extract the expected data for this output
    ##
    if config["run_config"]["expected_is_observed"]:
    # map the "expected" to be the same as the "observed"
        dut._log.warning(
            "WARNING Taking expected events to be the same as the observed events!"
        )
        output_testvector_file = "expected_is_observed"
        expected_output_events = recvd_events_intf
    else:
        output_testvector_file = master_tv_file
        expected_output_events = output_tv_list


    pass_count = 0
    fail_count = 0
    field_fail_cnt_header = []
    field_fail_cnt        = []
    field_fail_cnt_header.clear()
    field_fail_cnt.clear()


    for n_op_intf in range (CsfPorts.n_output_interfaces):
        events_are_equal, pass_count_i , fail_count_i, field_fail_count_i  = events.compare_BitFields(tv_bcid_list,
                                                                                                      output_tvformats[n_op_intf],
                                                                                                      CsfPorts.get_output_interface_ports(n_op_intf) ,
                                                                                                      num_events_to_process ,
                                                                                                      recvd_events_intf[n_op_intf],
                                                                                                      CsfPorts.config_outputs['tolerance'][n_op_intf],
                                                                                                      output_dir,
                                                                                                      stationNum=events.station_list_name_to_id(CsfPorts.config_outputs['station_id'][n_op_intf]));
        all_tests_passed = (all_tests_passed and events_are_equal)
        pass_count       = pass_count + pass_count_i
        fail_count       = fail_count + fail_count_i
        for key in field_fail_count_i.keys():
            field_fail_cnt_header.append([output_tvformats[n_op_intf] +" "+ "FIELDS: "+ key, "FAIL COUNT"])

        field_fail_cnt.append(field_fail_count_i)

    events.results_summary(
        num_events_to_process,
        pass_count,
        fail_count,
        CsfPorts.n_output_interfaces,
        field_fail_cnt_header,
        field_fail_cnt,
        total_ports=CsfPorts.n_output_ports()
    )

    cocotb_result = {True: cocotb.result.TestSuccess, False: cocotb.result.TestFailure}[
        all_tests_passed
    ]
    raise cocotb_result
