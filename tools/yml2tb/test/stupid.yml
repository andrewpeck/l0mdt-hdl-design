# common stuff copied from example
__config__:
    basic_convert_functions : off
    packages:
      shared_lib:
        - common_ieee_pkg

#############################################################
# L0MDT CONTROL TYPES
#############################################################  

HDL_types:
  - l0mdt_control:
    - clk       : { type: logic}
    - rst       : { type: logic}
    - bx        : { type: logic}
  
  - l0mdt_ttc:
    - bcr       : { type: logic}
    - ocr       : { type: logic}
    - ecr       : { type: logic}
    - l0a       : { type: logic}
    - l1a       : { type: logic}

