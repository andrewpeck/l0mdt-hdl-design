#################### upt Project ###########################
GEN:upt:
    extends: .generate_project
    variables:
      extends: .vars
      PROJECT_NAME: upt
      HOG_ONLY_SYNTH: 1 # if 1 runs only the synthesis
      HOG_CHECK_SYNTAX : 1

# SIM:upt:
#     extends: .simulate_project
#     variables:
#       extends: .vars
#       PROJECT_NAME: upt
