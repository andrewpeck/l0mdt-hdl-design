#################### hp Project ###########################
GEN:hp:
    extends: .generate_project
    variables:
      extends: .vars
      PROJECT_NAME: hp
      HOG_ONLY_SYNTH: 1 # if 1 runs only the synthesis
      HOG_CHECK_SYNTAX : 1

# SIM:hp:
#     extends: .simulate_project
#     variables:
#       extends: .vars
#       PROJECT_NAME: hp
