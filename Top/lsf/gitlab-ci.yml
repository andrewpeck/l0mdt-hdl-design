#################### LSF Project ###########################
GEN:lsf:
    extends: .GEN
    variables:
      extends: .vars
      PROJECT_NAME: lsf
      HOG_ONLY_SYNTH: 1 # if 1 runs only the synthesis
    tags:
      - normal-ops
      - hog
# SIM:example:
#     extends: .SIM
#     variables:
#       extends: .vars
#       PROJECT_NAME: example