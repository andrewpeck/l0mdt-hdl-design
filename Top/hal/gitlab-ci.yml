#################### hal Project ###########################
GEN:hal:
    extends: .generate_project
    variables:
      extends: .vars
      PROJECT_NAME: hal
      HOG_ONLY_SYNTH: 1 # if 1 runs only the synthesis
    tags:
      - normal-ops
      - hog
# SIM:hal:
#     extends: .simulate_project
#     variables:
#       extends: .vars
#       PROJECT_NAME: hal
