-- This file was auto-generated by YML2HDL toll.
-- https://gitlab.com/tcpaiva/yml2hdl

library ieee;

use ieee.std_logic_1164.all;
use ieee.numeric_std.all;

library shared_lib;

use shared_lib.common_ieee_pkg.all;
use shared_lib.l0mdt_constants_pkg.all;
use shared_lib.l0mdt_dataformats_pkg.all;
use shared_lib.common_constants_pkg.all;

package common_types_pkg is

  subtype bcid_t is unsigned(12-1 downto 0);

  subtype evid_t is unsigned(32-1 downto 0);

  subtype orid_t is unsigned(32-1 downto 0);

  type integer_bus_at is array(integer range <>) of integer;
  function len(x: integer_bus_at) return natural;
  function vectorify(x: integer_bus_at; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: integer_bus_at) return integer_bus_at;
  function nullify(x: integer_bus_at) return integer_bus_at;

  type l0mdt_control_rt is record
    clk : std_logic;
    rst : std_logic;
    bx : std_logic;
  end record l0mdt_control_rt;
  function len(x: l0mdt_control_rt) return natural;
  function vectorify(x: l0mdt_control_rt; t: std_logic_vector) return std_logic_vector;
  function structify(x: in std_logic_vector; t: l0mdt_control_rt) return l0mdt_control_rt;
  function nullify(t: l0mdt_control_rt) return l0mdt_control_rt;

  type l0mdt_ttc_rt is record
    bcr : std_logic;
    ocr : std_logic;
    ecr : std_logic;
    l0a : std_logic;
    l1a : std_logic;
    bcid : bcid_t;
    evid : evid_t;
    orid : orid_t;
  end record l0mdt_ttc_rt;
  function len(x: l0mdt_ttc_rt) return natural;
  function vectorify(x: l0mdt_ttc_rt; t: std_logic_vector) return std_logic_vector;
  function structify(x: in std_logic_vector; t: l0mdt_ttc_rt) return l0mdt_ttc_rt;
  function nullify(t: l0mdt_ttc_rt) return l0mdt_ttc_rt;

  type slc_rx_bus_at is array(integer range <>) of slc_rx_rt;
  function len(x: slc_rx_bus_at) return natural;
  function vectorify(x: slc_rx_bus_at; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: slc_rx_bus_at) return slc_rx_bus_at;
  function nullify(x: slc_rx_bus_at) return slc_rx_bus_at;

  type slc_rx_bus_art is array(integer range <>) of slc_rx_rt;
  function len(x: slc_rx_bus_art) return natural;
  function vectorify(x: slc_rx_bus_art; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: slc_rx_bus_art) return slc_rx_bus_art;
  function nullify(x: slc_rx_bus_art) return slc_rx_bus_art;

  type slc_rx_bus_avt is array(integer range <>) of slc_rx_vt;
  function len(x: slc_rx_bus_avt) return natural;
  function vectorify(x: slc_rx_bus_avt; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: slc_rx_bus_avt) return slc_rx_bus_avt;
  function nullify(x: slc_rx_bus_avt) return slc_rx_bus_avt;

  type slc_endcap_bus_at is array(integer range <>) of slc_endcap_rt;
  function len(x: slc_endcap_bus_at) return natural;
  function vectorify(x: slc_endcap_bus_at; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: slc_endcap_bus_at) return slc_endcap_bus_at;
  function nullify(x: slc_endcap_bus_at) return slc_endcap_bus_at;

  type slc_endcap_bus_art is array(integer range <>) of slc_endcap_rt;
  function len(x: slc_endcap_bus_art) return natural;
  function vectorify(x: slc_endcap_bus_art; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: slc_endcap_bus_art) return slc_endcap_bus_art;
  function nullify(x: slc_endcap_bus_art) return slc_endcap_bus_art;

  type slc_endcap_bus_avt is array(integer range <>) of slc_endcap_vt;
  function len(x: slc_endcap_bus_avt) return natural;
  function vectorify(x: slc_endcap_bus_avt; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: slc_endcap_bus_avt) return slc_endcap_bus_avt;
  function nullify(x: slc_endcap_bus_avt) return slc_endcap_bus_avt;

  type mdt_polmux_bus_art is array(integer range <>) of tdcpolmux2tar_rt;
  function len(x: mdt_polmux_bus_art) return natural;
  function vectorify(x: mdt_polmux_bus_art; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: mdt_polmux_bus_art) return mdt_polmux_bus_art;
  function nullify(x: mdt_polmux_bus_art) return mdt_polmux_bus_art;

  type mdt_polmux_bus_avt is array(integer range <>) of tdcpolmux2tar_vt;
  function len(x: mdt_polmux_bus_avt) return natural;
  function vectorify(x: mdt_polmux_bus_avt; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: mdt_polmux_bus_avt) return mdt_polmux_bus_avt;
  function nullify(x: mdt_polmux_bus_avt) return mdt_polmux_bus_avt;

  type tar2hps_bus_at is array(integer range <>) of tar2hps_rt;
  function len(x: tar2hps_bus_at) return natural;
  function vectorify(x: tar2hps_bus_at; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: tar2hps_bus_at) return tar2hps_bus_at;
  function nullify(x: tar2hps_bus_at) return tar2hps_bus_at;

  type tar2hps_bus_avt is array(integer range <>) of tar2hps_vt;
  function len(x: tar2hps_bus_avt) return natural;
  function vectorify(x: tar2hps_bus_avt; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: tar2hps_bus_avt) return tar2hps_bus_avt;
  function nullify(x: tar2hps_bus_avt) return tar2hps_bus_avt;

  type ucm2hps_bus_at is array(integer range <>) of ucm2hps_rt;
  function len(x: ucm2hps_bus_at) return natural;
  function vectorify(x: ucm2hps_bus_at; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: ucm2hps_bus_at) return ucm2hps_bus_at;
  function nullify(x: ucm2hps_bus_at) return ucm2hps_bus_at;

  type ucm2hps_bus_art is array(integer range <>) of ucm2hps_rt;
  function len(x: ucm2hps_bus_art) return natural;
  function vectorify(x: ucm2hps_bus_art; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: ucm2hps_bus_art) return ucm2hps_bus_art;
  function nullify(x: ucm2hps_bus_art) return ucm2hps_bus_art;

  type ucm2hps_bus_avt is array(integer range <>) of ucm2hps_vt;
  function len(x: ucm2hps_bus_avt) return natural;
  function vectorify(x: ucm2hps_bus_avt; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: ucm2hps_bus_avt) return ucm2hps_bus_avt;
  function nullify(x: ucm2hps_bus_avt) return ucm2hps_bus_avt;

  type heg2sfslc_bus_at is array(integer range <>) of heg2sfslc_rt;
  function len(x: heg2sfslc_bus_at) return natural;
  function vectorify(x: heg2sfslc_bus_at; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: heg2sfslc_bus_at) return heg2sfslc_bus_at;
  function nullify(x: heg2sfslc_bus_at) return heg2sfslc_bus_at;

  type heg2sfhit_bus_at is array(integer range <>) of heg2sfhit_rt;
  function len(x: heg2sfhit_bus_at) return natural;
  function vectorify(x: heg2sfhit_bus_at; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: heg2sfhit_bus_at) return heg2sfhit_bus_at;
  function nullify(x: heg2sfhit_bus_at) return heg2sfhit_bus_at;

  type sf2pt_bus_at is array(integer range <>) of sf2ptcalc_rt;
  function len(x: sf2pt_bus_at) return natural;
  function vectorify(x: sf2pt_bus_at; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: sf2pt_bus_at) return sf2pt_bus_at;
  function nullify(x: sf2pt_bus_at) return sf2pt_bus_at;

  type ucm2pl_bus_at is array(integer range <>) of ucm2pl_rt;
  function len(x: ucm2pl_bus_at) return natural;
  function vectorify(x: ucm2pl_bus_at; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: ucm2pl_bus_at) return ucm2pl_bus_at;
  function nullify(x: ucm2pl_bus_at) return ucm2pl_bus_at;

  type ucm2pl_bus_art is array(integer range <>) of ucm2pl_rt;
  function len(x: ucm2pl_bus_art) return natural;
  function vectorify(x: ucm2pl_bus_art; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: ucm2pl_bus_art) return ucm2pl_bus_art;
  function nullify(x: ucm2pl_bus_art) return ucm2pl_bus_art;

  type ucm2pl_bus_avt is array(integer range <>) of ucm2pl_vt;
  function len(x: ucm2pl_bus_avt) return natural;
  function vectorify(x: ucm2pl_bus_avt; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: ucm2pl_bus_avt) return ucm2pl_bus_avt;
  function nullify(x: ucm2pl_bus_avt) return ucm2pl_bus_avt;

  type pl2pt_bus_at is array(integer range <>) of pl2ptcalc_rt;
  function len(x: pl2pt_bus_at) return natural;
  function vectorify(x: pl2pt_bus_at; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: pl2pt_bus_at) return pl2pt_bus_at;
  function nullify(x: pl2pt_bus_at) return pl2pt_bus_at;

  type pl2mtc_bus_at is array(integer range <>) of pl2mtc_rt;
  function len(x: pl2mtc_bus_at) return natural;
  function vectorify(x: pl2mtc_bus_at; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: pl2mtc_bus_at) return pl2mtc_bus_at;
  function nullify(x: pl2mtc_bus_at) return pl2mtc_bus_at;

  type tf2mtc_bus_at is array(integer range <>) of ptcalc2mtc_rt;
  function len(x: tf2mtc_bus_at) return natural;
  function vectorify(x: tf2mtc_bus_at; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: tf2mtc_bus_at) return tf2mtc_bus_at;
  function nullify(x: tf2mtc_bus_at) return tf2mtc_bus_at;

  type mtc_out_bus_at is array(integer range <>) of mtc2sl_rt;
  function len(x: mtc_out_bus_at) return natural;
  function vectorify(x: mtc_out_bus_at; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: mtc_out_bus_at) return mtc_out_bus_at;
  function nullify(x: mtc_out_bus_at) return mtc_out_bus_at;

  type mtc2nsp_bus_at is array(integer range <>) of mtc2sl_rt;
  function len(x: mtc2nsp_bus_at) return natural;
  function vectorify(x: mtc2nsp_bus_at; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: mtc2nsp_bus_at) return mtc2nsp_bus_at;
  function nullify(x: mtc2nsp_bus_at) return mtc2nsp_bus_at;

  subtype felix_data_t is std_logic_vector(66-1 downto 0);

  type felix_stream_rt is record
    valid : std_logic;
    data : felix_data_t;
  end record felix_stream_rt;
  function len(x: felix_stream_rt) return natural;
  function vectorify(x: felix_stream_rt; t: std_logic_vector) return std_logic_vector;
  function structify(x: in std_logic_vector; t: felix_stream_rt) return felix_stream_rt;
  function nullify(t: felix_stream_rt) return felix_stream_rt;

  type felix_stream_bus_at is array(integer range <>) of felix_stream_rt;
  function len(x: felix_stream_bus_at) return natural;
  function vectorify(x: felix_stream_bus_at; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: felix_stream_bus_at) return felix_stream_bus_at;
  function nullify(x: felix_stream_bus_at) return felix_stream_bus_at;

end package common_types_pkg;

------------------------------------------------------------

package body common_types_pkg is

  function len(x: integer_bus_at) return natural is
    variable l : natural := 0;
  begin
    l := x'length * 32;
    return l;
  end function len;
  function vectorify(x: integer_bus_at; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l : integer := 32;
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: integer_bus_at) return integer_bus_at is
    variable y : integer_bus_at(t'range);
    constant l : integer := 32;
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: integer_bus_at) return integer_bus_at is
    variable y : integer_bus_at(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: l0mdt_control_rt) return natural is
    variable l : natural := 0;
  begin
    l := l + len(x.clk);
    l := l + len(x.rst);
    l := l + len(x.bx);
    return l;
  end function len;
  function vectorify(x: l0mdt_control_rt; t: std_logic_vector) return std_logic_vector is
    variable left : natural := t'left;
    variable y : std_logic_vector(t'range);
  begin
    if t'ascending then
      y(left to left+len(x.clk)-1) := vectorify(x.clk, y(left to left+len(x.clk)-1));
      left := left + len(x.clk);
      y(left to left+len(x.rst)-1) := vectorify(x.rst, y(left to left+len(x.rst)-1));
      left := left + len(x.rst);
      y(left to left+len(x.bx)-1) := vectorify(x.bx, y(left to left+len(x.bx)-1));
    else
      y(left downto left-len(x.clk)+1) := vectorify(x.clk, y(left downto left-len(x.clk)+1));
      left := left - len(x.clk);
      y(left downto left-len(x.rst)+1) := vectorify(x.rst, y(left downto left-len(x.rst)+1));
      left := left - len(x.rst);
      y(left downto left-len(x.bx)+1) := vectorify(x.bx, y(left downto left-len(x.bx)+1));
    end if;
    return y;
  end function vectorify;
  function structify(x: in std_logic_vector; t: l0mdt_control_rt) return l0mdt_control_rt is
    variable y: l0mdt_control_rt;
    variable left : natural := x'left;
  begin
    if x'ascending then
      y.clk := structify(x(left to left+len(y.clk)-1), y.clk);
      left := left + len(y.clk);
      y.rst := structify(x(left to left+len(y.rst)-1), y.rst);
      left := left + len(y.rst);
      y.bx := structify(x(left to left+len(y.bx)-1), y.bx);
    else
      y.clk := structify(x(left downto left-len(y.clk)+1), y.clk);
      left := left - len(y.clk);
      y.rst := structify(x(left downto left-len(y.rst)+1), y.rst);
      left := left - len(y.rst);
      y.bx := structify(x(left downto left-len(y.bx)+1), y.bx);
    end if;
    return y;
  end function structify;
  function nullify(t: l0mdt_control_rt) return l0mdt_control_rt is
  variable y: l0mdt_control_rt;
  begin
    y.clk := nullify(t.clk);
    y.rst := nullify(t.rst);
    y.bx := nullify(t.bx);
    return y;
  end function nullify;

  function len(x: l0mdt_ttc_rt) return natural is
    variable l : natural := 0;
  begin
    l := l + len(x.bcr);
    l := l + len(x.ocr);
    l := l + len(x.ecr);
    l := l + len(x.l0a);
    l := l + len(x.l1a);
    l := l + len(x.bcid);
    l := l + len(x.evid);
    l := l + len(x.orid);
    return l;
  end function len;
  function vectorify(x: l0mdt_ttc_rt; t: std_logic_vector) return std_logic_vector is
    variable left : natural := t'left;
    variable y : std_logic_vector(t'range);
  begin
    if t'ascending then
      y(left to left+len(x.bcr)-1) := vectorify(x.bcr, y(left to left+len(x.bcr)-1));
      left := left + len(x.bcr);
      y(left to left+len(x.ocr)-1) := vectorify(x.ocr, y(left to left+len(x.ocr)-1));
      left := left + len(x.ocr);
      y(left to left+len(x.ecr)-1) := vectorify(x.ecr, y(left to left+len(x.ecr)-1));
      left := left + len(x.ecr);
      y(left to left+len(x.l0a)-1) := vectorify(x.l0a, y(left to left+len(x.l0a)-1));
      left := left + len(x.l0a);
      y(left to left+len(x.l1a)-1) := vectorify(x.l1a, y(left to left+len(x.l1a)-1));
      left := left + len(x.l1a);
      y(left to left+len(x.bcid)-1) := vectorify(x.bcid, y(left to left+len(x.bcid)-1));
      left := left + len(x.bcid);
      y(left to left+len(x.evid)-1) := vectorify(x.evid, y(left to left+len(x.evid)-1));
      left := left + len(x.evid);
      y(left to left+len(x.orid)-1) := vectorify(x.orid, y(left to left+len(x.orid)-1));
    else
      y(left downto left-len(x.bcr)+1) := vectorify(x.bcr, y(left downto left-len(x.bcr)+1));
      left := left - len(x.bcr);
      y(left downto left-len(x.ocr)+1) := vectorify(x.ocr, y(left downto left-len(x.ocr)+1));
      left := left - len(x.ocr);
      y(left downto left-len(x.ecr)+1) := vectorify(x.ecr, y(left downto left-len(x.ecr)+1));
      left := left - len(x.ecr);
      y(left downto left-len(x.l0a)+1) := vectorify(x.l0a, y(left downto left-len(x.l0a)+1));
      left := left - len(x.l0a);
      y(left downto left-len(x.l1a)+1) := vectorify(x.l1a, y(left downto left-len(x.l1a)+1));
      left := left - len(x.l1a);
      y(left downto left-len(x.bcid)+1) := vectorify(x.bcid, y(left downto left-len(x.bcid)+1));
      left := left - len(x.bcid);
      y(left downto left-len(x.evid)+1) := vectorify(x.evid, y(left downto left-len(x.evid)+1));
      left := left - len(x.evid);
      y(left downto left-len(x.orid)+1) := vectorify(x.orid, y(left downto left-len(x.orid)+1));
    end if;
    return y;
  end function vectorify;
  function structify(x: in std_logic_vector; t: l0mdt_ttc_rt) return l0mdt_ttc_rt is
    variable y: l0mdt_ttc_rt;
    variable left : natural := x'left;
  begin
    if x'ascending then
      y.bcr := structify(x(left to left+len(y.bcr)-1), y.bcr);
      left := left + len(y.bcr);
      y.ocr := structify(x(left to left+len(y.ocr)-1), y.ocr);
      left := left + len(y.ocr);
      y.ecr := structify(x(left to left+len(y.ecr)-1), y.ecr);
      left := left + len(y.ecr);
      y.l0a := structify(x(left to left+len(y.l0a)-1), y.l0a);
      left := left + len(y.l0a);
      y.l1a := structify(x(left to left+len(y.l1a)-1), y.l1a);
      left := left + len(y.l1a);
      y.bcid := structify(x(left to left+len(y.bcid)-1), y.bcid);
      left := left + len(y.bcid);
      y.evid := structify(x(left to left+len(y.evid)-1), y.evid);
      left := left + len(y.evid);
      y.orid := structify(x(left to left+len(y.orid)-1), y.orid);
    else
      y.bcr := structify(x(left downto left-len(y.bcr)+1), y.bcr);
      left := left - len(y.bcr);
      y.ocr := structify(x(left downto left-len(y.ocr)+1), y.ocr);
      left := left - len(y.ocr);
      y.ecr := structify(x(left downto left-len(y.ecr)+1), y.ecr);
      left := left - len(y.ecr);
      y.l0a := structify(x(left downto left-len(y.l0a)+1), y.l0a);
      left := left - len(y.l0a);
      y.l1a := structify(x(left downto left-len(y.l1a)+1), y.l1a);
      left := left - len(y.l1a);
      y.bcid := structify(x(left downto left-len(y.bcid)+1), y.bcid);
      left := left - len(y.bcid);
      y.evid := structify(x(left downto left-len(y.evid)+1), y.evid);
      left := left - len(y.evid);
      y.orid := structify(x(left downto left-len(y.orid)+1), y.orid);
    end if;
    return y;
  end function structify;
  function nullify(t: l0mdt_ttc_rt) return l0mdt_ttc_rt is
  variable y: l0mdt_ttc_rt;
  begin
    y.bcr := nullify(t.bcr);
    y.ocr := nullify(t.ocr);
    y.ecr := nullify(t.ecr);
    y.l0a := nullify(t.l0a);
    y.l1a := nullify(t.l1a);
    y.bcid := nullify(t.bcid);
    y.evid := nullify(t.evid);
    y.orid := nullify(t.orid);
    return y;
  end function nullify;

  function len(x: slc_rx_bus_at) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: slc_rx_bus_at; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: slc_rx_bus_at) return slc_rx_bus_at is
    variable y : slc_rx_bus_at(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: slc_rx_bus_at) return slc_rx_bus_at is
    variable y : slc_rx_bus_at(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: slc_rx_bus_art) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: slc_rx_bus_art; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: slc_rx_bus_art) return slc_rx_bus_art is
    variable y : slc_rx_bus_art(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: slc_rx_bus_art) return slc_rx_bus_art is
    variable y : slc_rx_bus_art(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: slc_rx_bus_avt) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: slc_rx_bus_avt; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: slc_rx_bus_avt) return slc_rx_bus_avt is
    variable y : slc_rx_bus_avt(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: slc_rx_bus_avt) return slc_rx_bus_avt is
    variable y : slc_rx_bus_avt(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: slc_endcap_bus_at) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: slc_endcap_bus_at; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: slc_endcap_bus_at) return slc_endcap_bus_at is
    variable y : slc_endcap_bus_at(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: slc_endcap_bus_at) return slc_endcap_bus_at is
    variable y : slc_endcap_bus_at(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: slc_endcap_bus_art) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: slc_endcap_bus_art; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: slc_endcap_bus_art) return slc_endcap_bus_art is
    variable y : slc_endcap_bus_art(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: slc_endcap_bus_art) return slc_endcap_bus_art is
    variable y : slc_endcap_bus_art(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: slc_endcap_bus_avt) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: slc_endcap_bus_avt; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: slc_endcap_bus_avt) return slc_endcap_bus_avt is
    variable y : slc_endcap_bus_avt(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: slc_endcap_bus_avt) return slc_endcap_bus_avt is
    variable y : slc_endcap_bus_avt(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: mdt_polmux_bus_art) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: mdt_polmux_bus_art; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: mdt_polmux_bus_art) return mdt_polmux_bus_art is
    variable y : mdt_polmux_bus_art(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: mdt_polmux_bus_art) return mdt_polmux_bus_art is
    variable y : mdt_polmux_bus_art(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: mdt_polmux_bus_avt) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: mdt_polmux_bus_avt; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: mdt_polmux_bus_avt) return mdt_polmux_bus_avt is
    variable y : mdt_polmux_bus_avt(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: mdt_polmux_bus_avt) return mdt_polmux_bus_avt is
    variable y : mdt_polmux_bus_avt(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: tar2hps_bus_at) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: tar2hps_bus_at; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: tar2hps_bus_at) return tar2hps_bus_at is
    variable y : tar2hps_bus_at(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: tar2hps_bus_at) return tar2hps_bus_at is
    variable y : tar2hps_bus_at(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: tar2hps_bus_avt) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: tar2hps_bus_avt; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: tar2hps_bus_avt) return tar2hps_bus_avt is
    variable y : tar2hps_bus_avt(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: tar2hps_bus_avt) return tar2hps_bus_avt is
    variable y : tar2hps_bus_avt(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: ucm2hps_bus_at) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: ucm2hps_bus_at; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: ucm2hps_bus_at) return ucm2hps_bus_at is
    variable y : ucm2hps_bus_at(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: ucm2hps_bus_at) return ucm2hps_bus_at is
    variable y : ucm2hps_bus_at(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: ucm2hps_bus_art) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: ucm2hps_bus_art; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: ucm2hps_bus_art) return ucm2hps_bus_art is
    variable y : ucm2hps_bus_art(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: ucm2hps_bus_art) return ucm2hps_bus_art is
    variable y : ucm2hps_bus_art(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: ucm2hps_bus_avt) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: ucm2hps_bus_avt; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: ucm2hps_bus_avt) return ucm2hps_bus_avt is
    variable y : ucm2hps_bus_avt(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: ucm2hps_bus_avt) return ucm2hps_bus_avt is
    variable y : ucm2hps_bus_avt(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: heg2sfslc_bus_at) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: heg2sfslc_bus_at; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: heg2sfslc_bus_at) return heg2sfslc_bus_at is
    variable y : heg2sfslc_bus_at(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: heg2sfslc_bus_at) return heg2sfslc_bus_at is
    variable y : heg2sfslc_bus_at(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: heg2sfhit_bus_at) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: heg2sfhit_bus_at; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: heg2sfhit_bus_at) return heg2sfhit_bus_at is
    variable y : heg2sfhit_bus_at(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: heg2sfhit_bus_at) return heg2sfhit_bus_at is
    variable y : heg2sfhit_bus_at(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: sf2pt_bus_at) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: sf2pt_bus_at; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: sf2pt_bus_at) return sf2pt_bus_at is
    variable y : sf2pt_bus_at(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: sf2pt_bus_at) return sf2pt_bus_at is
    variable y : sf2pt_bus_at(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: ucm2pl_bus_at) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: ucm2pl_bus_at; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: ucm2pl_bus_at) return ucm2pl_bus_at is
    variable y : ucm2pl_bus_at(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: ucm2pl_bus_at) return ucm2pl_bus_at is
    variable y : ucm2pl_bus_at(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: ucm2pl_bus_art) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: ucm2pl_bus_art; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: ucm2pl_bus_art) return ucm2pl_bus_art is
    variable y : ucm2pl_bus_art(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: ucm2pl_bus_art) return ucm2pl_bus_art is
    variable y : ucm2pl_bus_art(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: ucm2pl_bus_avt) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: ucm2pl_bus_avt; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: ucm2pl_bus_avt) return ucm2pl_bus_avt is
    variable y : ucm2pl_bus_avt(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: ucm2pl_bus_avt) return ucm2pl_bus_avt is
    variable y : ucm2pl_bus_avt(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: pl2pt_bus_at) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: pl2pt_bus_at; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: pl2pt_bus_at) return pl2pt_bus_at is
    variable y : pl2pt_bus_at(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: pl2pt_bus_at) return pl2pt_bus_at is
    variable y : pl2pt_bus_at(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: pl2mtc_bus_at) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: pl2mtc_bus_at; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: pl2mtc_bus_at) return pl2mtc_bus_at is
    variable y : pl2mtc_bus_at(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: pl2mtc_bus_at) return pl2mtc_bus_at is
    variable y : pl2mtc_bus_at(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: tf2mtc_bus_at) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: tf2mtc_bus_at; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: tf2mtc_bus_at) return tf2mtc_bus_at is
    variable y : tf2mtc_bus_at(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: tf2mtc_bus_at) return tf2mtc_bus_at is
    variable y : tf2mtc_bus_at(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: mtc_out_bus_at) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: mtc_out_bus_at; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: mtc_out_bus_at) return mtc_out_bus_at is
    variable y : mtc_out_bus_at(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: mtc_out_bus_at) return mtc_out_bus_at is
    variable y : mtc_out_bus_at(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: mtc2nsp_bus_at) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: mtc2nsp_bus_at; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: mtc2nsp_bus_at) return mtc2nsp_bus_at is
    variable y : mtc2nsp_bus_at(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: mtc2nsp_bus_at) return mtc2nsp_bus_at is
    variable y : mtc2nsp_bus_at(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: felix_stream_rt) return natural is
    variable l : natural := 0;
  begin
    l := l + len(x.valid);
    l := l + len(x.data);
    return l;
  end function len;
  function vectorify(x: felix_stream_rt; t: std_logic_vector) return std_logic_vector is
    variable left : natural := t'left;
    variable y : std_logic_vector(t'range);
  begin
    if t'ascending then
      y(left to left+len(x.valid)-1) := vectorify(x.valid, y(left to left+len(x.valid)-1));
      left := left + len(x.valid);
      y(left to left+len(x.data)-1) := vectorify(x.data, y(left to left+len(x.data)-1));
    else
      y(left downto left-len(x.valid)+1) := vectorify(x.valid, y(left downto left-len(x.valid)+1));
      left := left - len(x.valid);
      y(left downto left-len(x.data)+1) := vectorify(x.data, y(left downto left-len(x.data)+1));
    end if;
    return y;
  end function vectorify;
  function structify(x: in std_logic_vector; t: felix_stream_rt) return felix_stream_rt is
    variable y: felix_stream_rt;
    variable left : natural := x'left;
  begin
    if x'ascending then
      y.valid := structify(x(left to left+len(y.valid)-1), y.valid);
      left := left + len(y.valid);
      y.data := structify(x(left to left+len(y.data)-1), y.data);
    else
      y.valid := structify(x(left downto left-len(y.valid)+1), y.valid);
      left := left - len(y.valid);
      y.data := structify(x(left downto left-len(y.data)+1), y.data);
    end if;
    return y;
  end function structify;
  function nullify(t: felix_stream_rt) return felix_stream_rt is
  variable y: felix_stream_rt;
  begin
    y.valid := nullify(t.valid);
    y.data := nullify(t.data);
    return y;
  end function nullify;

  function len(x: felix_stream_bus_at) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: felix_stream_bus_at; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: felix_stream_bus_at) return felix_stream_bus_at is
    variable y : felix_stream_bus_at(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: felix_stream_bus_at) return felix_stream_bus_at is
    variable y : felix_stream_bus_at(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

end package body common_types_pkg;
