-- This file was auto-generated by YML2HDL toll.
-- https://gitlab.com/tcpaiva/yml2hdl

library ieee;

use ieee.std_logic_1164.all;
use ieee.numeric_std.all;

library shared_lib;

use shared_lib.common_ieee_pkg.all;
use shared_lib.l0mdt_constants_pkg.all;
use shared_lib.l0mdt_dataformats_pkg.all;
use shared_lib.common_constants_pkg.all;
use shared_lib.common_types_pkg.all;

package ucm_pkg is

  constant UCM_INPUT_PL_LATENCY : integer := 2;

  constant UCM_OUTPUT_PL_LATENCY : integer := 2;

  constant SLC_SPECIFIC_LEN : integer := 85;

  type ucm_prepro2ctrl_rt is record
    data_valid : std_logic;
  end record ucm_prepro2ctrl_rt;
  function len(x: ucm_prepro2ctrl_rt) return natural;
  function vectorify(x: ucm_prepro2ctrl_rt; t: std_logic_vector) return std_logic_vector;
  function structify(x: in std_logic_vector; t: ucm_prepro2ctrl_rt) return ucm_prepro2ctrl_rt;
  function nullify(t: ucm_prepro2ctrl_rt) return ucm_prepro2ctrl_rt;

  subtype ucm_prepro2ctrl_vt is std_logic_vector(1-1 downto 0);

  subtype ucm_prepro2ctrl_rvt is std_logic_vector(1-1 downto 0);

  type ucm_prepro2ctrl_bus_art is array(integer range <>) of ucm_prepro2ctrl_rt;
  function len(x: ucm_prepro2ctrl_bus_art) return natural;
  function vectorify(x: ucm_prepro2ctrl_bus_art; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: ucm_prepro2ctrl_bus_art) return ucm_prepro2ctrl_bus_art;
  function nullify(x: ucm_prepro2ctrl_bus_art) return ucm_prepro2ctrl_bus_art;

  type ucm_prepro2ctrl_bus_at is array(integer range <>) of ucm_prepro2ctrl_rt;
  function len(x: ucm_prepro2ctrl_bus_at) return natural;
  function vectorify(x: ucm_prepro2ctrl_bus_at; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: ucm_prepro2ctrl_bus_at) return ucm_prepro2ctrl_bus_at;
  function nullify(x: ucm_prepro2ctrl_bus_at) return ucm_prepro2ctrl_bus_at;

  type ucm_prepro2ctrl_bus_avt is array(integer range <>) of ucm_prepro2ctrl_vt;
  function len(x: ucm_prepro2ctrl_bus_avt) return natural;
  function vectorify(x: ucm_prepro2ctrl_bus_avt; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: ucm_prepro2ctrl_bus_avt) return ucm_prepro2ctrl_bus_avt;
  function nullify(x: ucm_prepro2ctrl_bus_avt) return ucm_prepro2ctrl_bus_avt;

  subtype chamb_ieta_rpc_t is unsigned(4-1 downto 0);

  type chamb_ieta_rpc_bus_at is array(4-1 downto 0) of chamb_ieta_rpc_t;
  function len(x: chamb_ieta_rpc_bus_at) return natural;
  function vectorify(x: chamb_ieta_rpc_bus_at; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: chamb_ieta_rpc_bus_at) return chamb_ieta_rpc_bus_at;
  function nullify(x: chamb_ieta_rpc_bus_at) return chamb_ieta_rpc_bus_at;

  type ucm_cde_rt is record
    muid : slc_muid_rt;
    mdtid : vec_mdtid_rt;
    chamb_ieta : chamb_ieta_rpc_bus_at;
    cointype : std_logic_vector(SLC_COMMON_COINTYPE_LEN-1 downto 0);
    specific : std_logic_vector(SLC_SPECIFIC_LEN-1 downto 0);
    data_valid : std_logic;
  end record ucm_cde_rt;
  function len(x: ucm_cde_rt) return natural;
  function vectorify(x: ucm_cde_rt; t: std_logic_vector) return std_logic_vector;
  function structify(x: in std_logic_vector; t: ucm_cde_rt) return ucm_cde_rt;
  function nullify(t: ucm_cde_rt) return ucm_cde_rt;

  subtype ucm_cde_vt is std_logic_vector(135-1 downto 0);

  subtype ucm_cde_rvt is std_logic_vector(135-1 downto 0);

  type ucm_cde_bus_at is array(integer range <>) of ucm_cde_rt;
  function len(x: ucm_cde_bus_at) return natural;
  function vectorify(x: ucm_cde_bus_at; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: ucm_cde_bus_at) return ucm_cde_bus_at;
  function nullify(x: ucm_cde_bus_at) return ucm_cde_bus_at;

  type ucm_cde_bus_avt is array(integer range <>) of ucm_cde_vt;
  function len(x: ucm_cde_bus_avt) return natural;
  function vectorify(x: ucm_cde_bus_avt; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: ucm_cde_bus_avt) return ucm_cde_bus_avt;
  function nullify(x: ucm_cde_bus_avt) return ucm_cde_bus_avt;

  type ucm_csw_ch_control_rt is record
    data_present : std_logic;
    addr_orig : std_logic_vector(4-1 downto 0);
  end record ucm_csw_ch_control_rt;
  function len(x: ucm_csw_ch_control_rt) return natural;
  function vectorify(x: ucm_csw_ch_control_rt; t: std_logic_vector) return std_logic_vector;
  function structify(x: in std_logic_vector; t: ucm_csw_ch_control_rt) return ucm_csw_ch_control_rt;
  function nullify(t: ucm_csw_ch_control_rt) return ucm_csw_ch_control_rt;

  type ucm_csw_control_art is array(integer range <>) of ucm_csw_ch_control_rt;
  function len(x: ucm_csw_control_art) return natural;
  function vectorify(x: ucm_csw_control_art; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: ucm_csw_control_art) return ucm_csw_control_art;
  function nullify(x: ucm_csw_control_art) return ucm_csw_control_art;

  type ucm_csw_control_at is array(integer range <>) of ucm_csw_ch_control_rt;
  function len(x: ucm_csw_control_at) return natural;
  function vectorify(x: ucm_csw_control_at; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: ucm_csw_control_at) return ucm_csw_control_at;
  function nullify(x: ucm_csw_control_at) return ucm_csw_control_at;

  type ucm_pam_ch_control_rt is record
    data_present : std_logic;
    addr_orig : std_logic_vector(4-1 downto 0);
  end record ucm_pam_ch_control_rt;
  function len(x: ucm_pam_ch_control_rt) return natural;
  function vectorify(x: ucm_pam_ch_control_rt; t: std_logic_vector) return std_logic_vector;
  function structify(x: in std_logic_vector; t: ucm_pam_ch_control_rt) return ucm_pam_ch_control_rt;
  function nullify(t: ucm_pam_ch_control_rt) return ucm_pam_ch_control_rt;

  type ucm_pam_control_art is array(integer range <>) of ucm_pam_ch_control_rt;
  function len(x: ucm_pam_control_art) return natural;
  function vectorify(x: ucm_pam_control_art; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: ucm_pam_control_art) return ucm_pam_control_art;
  function nullify(x: ucm_pam_control_art) return ucm_pam_control_art;

  type ucm_pam_control_at is array(integer range <>) of ucm_pam_ch_control_rt;
  function len(x: ucm_pam_control_at) return natural;
  function vectorify(x: ucm_pam_control_at; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: ucm_pam_control_at) return ucm_pam_control_at;
  function nullify(x: ucm_pam_control_at) return ucm_pam_control_at;

  type ucm_proc_info_ch_rt is record
    ch : std_logic_vector(4-1 downto 0);
    processed : std_logic;
  end record ucm_proc_info_ch_rt;
  function len(x: ucm_proc_info_ch_rt) return natural;
  function vectorify(x: ucm_proc_info_ch_rt; t: std_logic_vector) return std_logic_vector;
  function structify(x: in std_logic_vector; t: ucm_proc_info_ch_rt) return ucm_proc_info_ch_rt;
  function nullify(t: ucm_proc_info_ch_rt) return ucm_proc_info_ch_rt;

  type ucm_proc_info_art is array(integer range <>) of ucm_proc_info_ch_rt;
  function len(x: ucm_proc_info_art) return natural;
  function vectorify(x: ucm_proc_info_art; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: ucm_proc_info_art) return ucm_proc_info_art;
  function nullify(x: ucm_proc_info_art) return ucm_proc_info_art;

  type ucm_proc_info_at is array(integer range <>) of ucm_proc_info_ch_rt;
  function len(x: ucm_proc_info_at) return natural;
  function vectorify(x: ucm_proc_info_at; t: std_logic_vector) return std_logic_vector;
  function structify(x: std_logic_vector; t: ucm_proc_info_at) return ucm_proc_info_at;
  function nullify(x: ucm_proc_info_at) return ucm_proc_info_at;

end package ucm_pkg;

------------------------------------------------------------

package body ucm_pkg is

  function len(x: ucm_prepro2ctrl_rt) return natural is
    variable l : natural := 0;
  begin
    l := l + len(x.data_valid);
    return l;
  end function len;
  function vectorify(x: ucm_prepro2ctrl_rt; t: std_logic_vector) return std_logic_vector is
    variable left : natural := t'left;
    variable y : std_logic_vector(t'range);
  begin
    if t'ascending then
      y(left to left+len(x.data_valid)-1) := vectorify(x.data_valid, y(left to left+len(x.data_valid)-1));
    else
      y(left downto left-len(x.data_valid)+1) := vectorify(x.data_valid, y(left downto left-len(x.data_valid)+1));
    end if;
    return y;
  end function vectorify;
  function structify(x: in std_logic_vector; t: ucm_prepro2ctrl_rt) return ucm_prepro2ctrl_rt is
    variable y: ucm_prepro2ctrl_rt;
    variable left : natural := x'left;
  begin
    if x'ascending then
      y.data_valid := structify(x(left to left+len(y.data_valid)-1), y.data_valid);
    else
      y.data_valid := structify(x(left downto left-len(y.data_valid)+1), y.data_valid);
    end if;
    return y;
  end function structify;
  function nullify(t: ucm_prepro2ctrl_rt) return ucm_prepro2ctrl_rt is
  variable y: ucm_prepro2ctrl_rt;
  begin
    y.data_valid := nullify(t.data_valid);
    return y;
  end function nullify;

  function len(x: ucm_prepro2ctrl_bus_art) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: ucm_prepro2ctrl_bus_art; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: ucm_prepro2ctrl_bus_art) return ucm_prepro2ctrl_bus_art is
    variable y : ucm_prepro2ctrl_bus_art(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: ucm_prepro2ctrl_bus_art) return ucm_prepro2ctrl_bus_art is
    variable y : ucm_prepro2ctrl_bus_art(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: ucm_prepro2ctrl_bus_at) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: ucm_prepro2ctrl_bus_at; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: ucm_prepro2ctrl_bus_at) return ucm_prepro2ctrl_bus_at is
    variable y : ucm_prepro2ctrl_bus_at(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: ucm_prepro2ctrl_bus_at) return ucm_prepro2ctrl_bus_at is
    variable y : ucm_prepro2ctrl_bus_at(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: ucm_prepro2ctrl_bus_avt) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: ucm_prepro2ctrl_bus_avt; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: ucm_prepro2ctrl_bus_avt) return ucm_prepro2ctrl_bus_avt is
    variable y : ucm_prepro2ctrl_bus_avt(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: ucm_prepro2ctrl_bus_avt) return ucm_prepro2ctrl_bus_avt is
    variable y : ucm_prepro2ctrl_bus_avt(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: chamb_ieta_rpc_bus_at) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: chamb_ieta_rpc_bus_at; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: chamb_ieta_rpc_bus_at) return chamb_ieta_rpc_bus_at is
    variable y : chamb_ieta_rpc_bus_at;
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: chamb_ieta_rpc_bus_at) return chamb_ieta_rpc_bus_at is
    variable y : chamb_ieta_rpc_bus_at;
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: ucm_cde_rt) return natural is
    variable l : natural := 0;
  begin
    l := l + len(x.muid);
    l := l + len(x.mdtid);
    l := l + len(x.chamb_ieta);
    l := l + len(x.cointype);
    l := l + len(x.specific);
    l := l + len(x.data_valid);
    return l;
  end function len;
  function vectorify(x: ucm_cde_rt; t: std_logic_vector) return std_logic_vector is
    variable left : natural := t'left;
    variable y : std_logic_vector(t'range);
  begin
    if t'ascending then
      y(left to left+len(x.muid)-1) := vectorify(x.muid, y(left to left+len(x.muid)-1));
      left := left + len(x.muid);
      y(left to left+len(x.mdtid)-1) := vectorify(x.mdtid, y(left to left+len(x.mdtid)-1));
      left := left + len(x.mdtid);
      y(left to left+len(x.chamb_ieta)-1) := vectorify(x.chamb_ieta, y(left to left+len(x.chamb_ieta)-1));
      left := left + len(x.chamb_ieta);
      y(left to left+len(x.cointype)-1) := vectorify(x.cointype, y(left to left+len(x.cointype)-1));
      left := left + len(x.cointype);
      y(left to left+len(x.specific)-1) := vectorify(x.specific, y(left to left+len(x.specific)-1));
      left := left + len(x.specific);
      y(left to left+len(x.data_valid)-1) := vectorify(x.data_valid, y(left to left+len(x.data_valid)-1));
    else
      y(left downto left-len(x.muid)+1) := vectorify(x.muid, y(left downto left-len(x.muid)+1));
      left := left - len(x.muid);
      y(left downto left-len(x.mdtid)+1) := vectorify(x.mdtid, y(left downto left-len(x.mdtid)+1));
      left := left - len(x.mdtid);
      y(left downto left-len(x.chamb_ieta)+1) := vectorify(x.chamb_ieta, y(left downto left-len(x.chamb_ieta)+1));
      left := left - len(x.chamb_ieta);
      y(left downto left-len(x.cointype)+1) := vectorify(x.cointype, y(left downto left-len(x.cointype)+1));
      left := left - len(x.cointype);
      y(left downto left-len(x.specific)+1) := vectorify(x.specific, y(left downto left-len(x.specific)+1));
      left := left - len(x.specific);
      y(left downto left-len(x.data_valid)+1) := vectorify(x.data_valid, y(left downto left-len(x.data_valid)+1));
    end if;
    return y;
  end function vectorify;
  function structify(x: in std_logic_vector; t: ucm_cde_rt) return ucm_cde_rt is
    variable y: ucm_cde_rt;
    variable left : natural := x'left;
  begin
    if x'ascending then
      y.muid := structify(x(left to left+len(y.muid)-1), y.muid);
      left := left + len(y.muid);
      y.mdtid := structify(x(left to left+len(y.mdtid)-1), y.mdtid);
      left := left + len(y.mdtid);
      y.chamb_ieta := structify(x(left to left+len(y.chamb_ieta)-1), y.chamb_ieta);
      left := left + len(y.chamb_ieta);
      y.cointype := structify(x(left to left+len(y.cointype)-1), y.cointype);
      left := left + len(y.cointype);
      y.specific := structify(x(left to left+len(y.specific)-1), y.specific);
      left := left + len(y.specific);
      y.data_valid := structify(x(left to left+len(y.data_valid)-1), y.data_valid);
    else
      y.muid := structify(x(left downto left-len(y.muid)+1), y.muid);
      left := left - len(y.muid);
      y.mdtid := structify(x(left downto left-len(y.mdtid)+1), y.mdtid);
      left := left - len(y.mdtid);
      y.chamb_ieta := structify(x(left downto left-len(y.chamb_ieta)+1), y.chamb_ieta);
      left := left - len(y.chamb_ieta);
      y.cointype := structify(x(left downto left-len(y.cointype)+1), y.cointype);
      left := left - len(y.cointype);
      y.specific := structify(x(left downto left-len(y.specific)+1), y.specific);
      left := left - len(y.specific);
      y.data_valid := structify(x(left downto left-len(y.data_valid)+1), y.data_valid);
    end if;
    return y;
  end function structify;
  function nullify(t: ucm_cde_rt) return ucm_cde_rt is
  variable y: ucm_cde_rt;
  begin
    y.muid := nullify(t.muid);
    y.mdtid := nullify(t.mdtid);
    y.chamb_ieta := nullify(t.chamb_ieta);
    y.cointype := nullify(t.cointype);
    y.specific := nullify(t.specific);
    y.data_valid := nullify(t.data_valid);
    return y;
  end function nullify;

  function len(x: ucm_cde_bus_at) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: ucm_cde_bus_at; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: ucm_cde_bus_at) return ucm_cde_bus_at is
    variable y : ucm_cde_bus_at(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: ucm_cde_bus_at) return ucm_cde_bus_at is
    variable y : ucm_cde_bus_at(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: ucm_cde_bus_avt) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: ucm_cde_bus_avt; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: ucm_cde_bus_avt) return ucm_cde_bus_avt is
    variable y : ucm_cde_bus_avt(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: ucm_cde_bus_avt) return ucm_cde_bus_avt is
    variable y : ucm_cde_bus_avt(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: ucm_csw_ch_control_rt) return natural is
    variable l : natural := 0;
  begin
    l := l + len(x.data_present);
    l := l + len(x.addr_orig);
    return l;
  end function len;
  function vectorify(x: ucm_csw_ch_control_rt; t: std_logic_vector) return std_logic_vector is
    variable left : natural := t'left;
    variable y : std_logic_vector(t'range);
  begin
    if t'ascending then
      y(left to left+len(x.data_present)-1) := vectorify(x.data_present, y(left to left+len(x.data_present)-1));
      left := left + len(x.data_present);
      y(left to left+len(x.addr_orig)-1) := vectorify(x.addr_orig, y(left to left+len(x.addr_orig)-1));
    else
      y(left downto left-len(x.data_present)+1) := vectorify(x.data_present, y(left downto left-len(x.data_present)+1));
      left := left - len(x.data_present);
      y(left downto left-len(x.addr_orig)+1) := vectorify(x.addr_orig, y(left downto left-len(x.addr_orig)+1));
    end if;
    return y;
  end function vectorify;
  function structify(x: in std_logic_vector; t: ucm_csw_ch_control_rt) return ucm_csw_ch_control_rt is
    variable y: ucm_csw_ch_control_rt;
    variable left : natural := x'left;
  begin
    if x'ascending then
      y.data_present := structify(x(left to left+len(y.data_present)-1), y.data_present);
      left := left + len(y.data_present);
      y.addr_orig := structify(x(left to left+len(y.addr_orig)-1), y.addr_orig);
    else
      y.data_present := structify(x(left downto left-len(y.data_present)+1), y.data_present);
      left := left - len(y.data_present);
      y.addr_orig := structify(x(left downto left-len(y.addr_orig)+1), y.addr_orig);
    end if;
    return y;
  end function structify;
  function nullify(t: ucm_csw_ch_control_rt) return ucm_csw_ch_control_rt is
  variable y: ucm_csw_ch_control_rt;
  begin
    y.data_present := nullify(t.data_present);
    y.addr_orig := nullify(t.addr_orig);
    return y;
  end function nullify;

  function len(x: ucm_csw_control_art) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: ucm_csw_control_art; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: ucm_csw_control_art) return ucm_csw_control_art is
    variable y : ucm_csw_control_art(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: ucm_csw_control_art) return ucm_csw_control_art is
    variable y : ucm_csw_control_art(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: ucm_csw_control_at) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: ucm_csw_control_at; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: ucm_csw_control_at) return ucm_csw_control_at is
    variable y : ucm_csw_control_at(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: ucm_csw_control_at) return ucm_csw_control_at is
    variable y : ucm_csw_control_at(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: ucm_pam_ch_control_rt) return natural is
    variable l : natural := 0;
  begin
    l := l + len(x.data_present);
    l := l + len(x.addr_orig);
    return l;
  end function len;
  function vectorify(x: ucm_pam_ch_control_rt; t: std_logic_vector) return std_logic_vector is
    variable left : natural := t'left;
    variable y : std_logic_vector(t'range);
  begin
    if t'ascending then
      y(left to left+len(x.data_present)-1) := vectorify(x.data_present, y(left to left+len(x.data_present)-1));
      left := left + len(x.data_present);
      y(left to left+len(x.addr_orig)-1) := vectorify(x.addr_orig, y(left to left+len(x.addr_orig)-1));
    else
      y(left downto left-len(x.data_present)+1) := vectorify(x.data_present, y(left downto left-len(x.data_present)+1));
      left := left - len(x.data_present);
      y(left downto left-len(x.addr_orig)+1) := vectorify(x.addr_orig, y(left downto left-len(x.addr_orig)+1));
    end if;
    return y;
  end function vectorify;
  function structify(x: in std_logic_vector; t: ucm_pam_ch_control_rt) return ucm_pam_ch_control_rt is
    variable y: ucm_pam_ch_control_rt;
    variable left : natural := x'left;
  begin
    if x'ascending then
      y.data_present := structify(x(left to left+len(y.data_present)-1), y.data_present);
      left := left + len(y.data_present);
      y.addr_orig := structify(x(left to left+len(y.addr_orig)-1), y.addr_orig);
    else
      y.data_present := structify(x(left downto left-len(y.data_present)+1), y.data_present);
      left := left - len(y.data_present);
      y.addr_orig := structify(x(left downto left-len(y.addr_orig)+1), y.addr_orig);
    end if;
    return y;
  end function structify;
  function nullify(t: ucm_pam_ch_control_rt) return ucm_pam_ch_control_rt is
  variable y: ucm_pam_ch_control_rt;
  begin
    y.data_present := nullify(t.data_present);
    y.addr_orig := nullify(t.addr_orig);
    return y;
  end function nullify;

  function len(x: ucm_pam_control_art) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: ucm_pam_control_art; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: ucm_pam_control_art) return ucm_pam_control_art is
    variable y : ucm_pam_control_art(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: ucm_pam_control_art) return ucm_pam_control_art is
    variable y : ucm_pam_control_art(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: ucm_pam_control_at) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: ucm_pam_control_at; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: ucm_pam_control_at) return ucm_pam_control_at is
    variable y : ucm_pam_control_at(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: ucm_pam_control_at) return ucm_pam_control_at is
    variable y : ucm_pam_control_at(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: ucm_proc_info_ch_rt) return natural is
    variable l : natural := 0;
  begin
    l := l + len(x.ch);
    l := l + len(x.processed);
    return l;
  end function len;
  function vectorify(x: ucm_proc_info_ch_rt; t: std_logic_vector) return std_logic_vector is
    variable left : natural := t'left;
    variable y : std_logic_vector(t'range);
  begin
    if t'ascending then
      y(left to left+len(x.ch)-1) := vectorify(x.ch, y(left to left+len(x.ch)-1));
      left := left + len(x.ch);
      y(left to left+len(x.processed)-1) := vectorify(x.processed, y(left to left+len(x.processed)-1));
    else
      y(left downto left-len(x.ch)+1) := vectorify(x.ch, y(left downto left-len(x.ch)+1));
      left := left - len(x.ch);
      y(left downto left-len(x.processed)+1) := vectorify(x.processed, y(left downto left-len(x.processed)+1));
    end if;
    return y;
  end function vectorify;
  function structify(x: in std_logic_vector; t: ucm_proc_info_ch_rt) return ucm_proc_info_ch_rt is
    variable y: ucm_proc_info_ch_rt;
    variable left : natural := x'left;
  begin
    if x'ascending then
      y.ch := structify(x(left to left+len(y.ch)-1), y.ch);
      left := left + len(y.ch);
      y.processed := structify(x(left to left+len(y.processed)-1), y.processed);
    else
      y.ch := structify(x(left downto left-len(y.ch)+1), y.ch);
      left := left - len(y.ch);
      y.processed := structify(x(left downto left-len(y.processed)+1), y.processed);
    end if;
    return y;
  end function structify;
  function nullify(t: ucm_proc_info_ch_rt) return ucm_proc_info_ch_rt is
  variable y: ucm_proc_info_ch_rt;
  begin
    y.ch := nullify(t.ch);
    y.processed := nullify(t.processed);
    return y;
  end function nullify;

  function len(x: ucm_proc_info_art) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: ucm_proc_info_art; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: ucm_proc_info_art) return ucm_proc_info_art is
    variable y : ucm_proc_info_art(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: ucm_proc_info_art) return ucm_proc_info_art is
    variable y : ucm_proc_info_art(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

  function len(x: ucm_proc_info_at) return natural is
    variable l : natural := 0;
  begin
    l := x'length * len(x(x'left));
    return l;
  end function len;
  function vectorify(x: ucm_proc_info_at; t: std_logic_vector) return std_logic_vector is
    variable y : std_logic_vector(t'range);
    constant l :  integer := len(x(x'right));
    variable a :  integer;
    variable b :  integer;
  begin
    if t'ascending then
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(b to a) := vectorify(x(i), y(b to a));
      end loop;
    else
      for i in x'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(a downto b) := vectorify(x(i), y(a downto b));
      end loop;
    end if;
    return y;
  end function vectorify;
  function structify(x: std_logic_vector; t: ucm_proc_info_at) return ucm_proc_info_at is
    variable y : ucm_proc_info_at(t'range);
    constant l :  integer := len(y(y'left));
    variable a :  integer;
    variable b :  integer;
  begin
    if x'ascending then
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(b to a), y(i));
      end loop;
    else
      for i in y'range loop
        a := l*i+l-1+x'low;
        b := l*i+x'low;
        y(i) := structify(x(a downto b), y(i));
      end loop;
    end if;
    return y;
  end function structify;
  function nullify(x: ucm_proc_info_at) return ucm_proc_info_at is
    variable y : ucm_proc_info_at(x'range);
  begin
    l: for i in y'range loop
      y(i) := nullify(y(i));
    end loop l;
    return y;
  end function nullify;

end package body ucm_pkg;
